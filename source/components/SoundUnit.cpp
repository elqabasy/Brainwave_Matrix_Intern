#include "components/SoundUnit.hpp"
#include <iostream>

void SoundUnit::playSound(const string& sound) const {
    // cout << "Playing sound: " << sound << endl;
    // here we can connect to sound unit component
    // i don't know how, but still we can in the future
    // i make a simple simulation depend on some software engineering practice
}
